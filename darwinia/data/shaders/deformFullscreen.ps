#include "globals.h"

sampler screenSampler;
sampler deformSampler;

float4 ps_main(in float2 iUv : TEXCOORD0) : COLOR
{
	float4 delta = tex2D(deformSampler,iUv);
	return tex2D(screenSampler,iUv + (delta.xy-delta.zw)/DEFORM_PRECISION);
}
